You will be given a question and your task is to extract only the necessary keywords from the question, which will be used to search a database. 
If there are multiple important keywords, separate them with commas, but do NOT split keywords that belong to the same phrase.
ONLY extract keywords that will be useful for search and avoid generic terms and words.
If one or more Named Entities are present, extract ONLY those and nothing else.
If no Named Entities exist in the question, extract the most relevant keywords.